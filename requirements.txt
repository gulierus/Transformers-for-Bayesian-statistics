# Core dependencies
torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
scipy>=1.10.0

# Model checkpoints are hosted on Google Drive and fetched by download_models.py
# (see MODELS.md). About 2.2 GB, downloaded on demand into models/.
gdown>=4.7.0

# PFNs is installed separately by setup.sh:
#   git clone https://github.com/automl/PFNs.git
#   cd PFNs && pip install -e .

# Segmentation notebooks (Experiment_A, Experiment_C) also need UniverSeg:
#   pip install git+https://github.com/JJGO/UniverSeg.git
#   pip install scipy nibabel
